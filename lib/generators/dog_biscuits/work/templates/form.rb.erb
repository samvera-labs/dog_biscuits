# Generated via
#  `rails generate dog_biscuits:work <%= class_name %>`
module Hyrax
  class <%= class_name %>Form < Hyrax::Forms::WorkForm
    self.model_class = ::<%= class_name %>

    self.terms -= self.terms -= DogBiscuits.config.base_properties
    self.terms += DogBiscuits.config.<%= class_name.underscore %>_properties

    # Add any local properties here
    self.terms += []

    self.required_fields = DogBiscuits.config.<%= class_name.underscore %>_properties_required
  end
end
